"""Generated from Smithy shape ``com.amazonaws.cloudformation#DeleteStackInput``."""

from typing import TYPE_CHECKING

from typing_extensions import NotRequired, TypedDict

from aws_sdk_cloudformation._protocol.xml import Element

if TYPE_CHECKING:
    import aws_sdk_cloudformation.types.client_request_token
    import aws_sdk_cloudformation.types.deletion_mode
    import aws_sdk_cloudformation.types.retain_resources
    import aws_sdk_cloudformation.types.role_arn
    import aws_sdk_cloudformation.types.stack_name


class DeleteStackInput(TypedDict, closed=True):
    stack_name: NotRequired["aws_sdk_cloudformation.types.stack_name.StackName"]
    """<p>The name or the unique stack ID that's associated with the stack.</p>"""
    retain_resources: NotRequired[
        "aws_sdk_cloudformation.types.retain_resources.RetainResources"
    ]
    """<p>For stacks in the <code>DELETE_FAILED</code> state, a list of resource logical IDs that are associated with the resources you want to retain. During deletion, CloudFormation deletes the stack but doesn't delete the retained resources.</p> <p>Retaining resources is useful when you can't delete a resource, such as a non-empty S3 bucket, but you want to delete the stack.</p>"""
    role_arn: NotRequired["aws_sdk_cloudformation.types.role_arn.RoleARN"]
    """<p>The Amazon Resource Name (ARN) of an IAM role that CloudFormation assumes to delete the stack. CloudFormation uses the role's credentials to make calls on your behalf.</p> <p>If you don't specify a value, CloudFormation uses the role that was previously associated with the stack. If no role is available, CloudFormation uses a temporary session that's generated from your user credentials.</p>"""
    client_request_token: NotRequired[
        "aws_sdk_cloudformation.types.client_request_token.ClientRequestToken"
    ]
    """<p>A unique identifier for this <code>DeleteStack</code> request. Specify this token if you plan to retry requests so that CloudFormation knows that you're not attempting to delete a stack with the same name. You might retry <code>DeleteStack</code> requests to ensure that CloudFormation successfully received them.</p> <p>All events initiated by a given stack operation are assigned the same client request token, which you can use to track operations. For example, if you execute a <code>CreateStack</code> operation with the token <code>token1</code>, then all the <code>StackEvents</code> generated by that operation will have <code>ClientRequestToken</code> set as <code>token1</code>.</p> <p>In the console, stack operations display the client request token on the Events tab. Stack operations that are initiated from the console use the token format <i>Console-StackOperation-ID</i>, which helps you easily identify the stack operation . For example, if you create a stack using the console, each stack event would be assigned the same token in the following format: <code>Console-CreateStack-7f59c3cf-00d2-40c7-b2ff-e75db0987002</code>.</p>"""
    deletion_mode: NotRequired[
        "aws_sdk_cloudformation.types.deletion_mode.DeletionMode"
    ]
    """<p>Specifies the deletion mode for the stack. Possible values are:</p> <ul> <li> <p> <code>STANDARD</code> - Use the standard behavior. Specifying this value is the same as not specifying this parameter.</p> </li> <li> <p> <code>FORCE_DELETE_STACK</code> - Delete the stack if it's stuck in a <code>DELETE_FAILED</code> state due to resource deletion failure.</p> </li> </ul>"""


# --- awsQuery ser/de ---
def serialize_query(
    value: DeleteStackInput, pairs: list[tuple[str, str]], prefix: str
) -> None:
    if "stack_name" in value:
        pairs.append((f"{prefix}.StackName", str(value["stack_name"])))
    if "retain_resources" in value:
        import aws_sdk_cloudformation.types.retain_resources

        aws_sdk_cloudformation.types.retain_resources.serialize_query(
            value["retain_resources"], pairs, f"{prefix}.RetainResources"
        )
    if "role_arn" in value:
        pairs.append((f"{prefix}.RoleARN", str(value["role_arn"])))
    if "client_request_token" in value:
        pairs.append(
            (f"{prefix}.ClientRequestToken", str(value["client_request_token"]))
        )
    if "deletion_mode" in value:
        import aws_sdk_cloudformation.types.deletion_mode

        aws_sdk_cloudformation.types.deletion_mode.serialize_query(
            value["deletion_mode"], pairs, f"{prefix}.DeletionMode"
        )


def deserialize_query(el: Element) -> DeleteStackInput:
    out: DeleteStackInput = {}  # type: ignore[typeddict-item]
    child_stack_name = el.find("StackName")
    if child_stack_name is not None:
        out["stack_name"] = str(child_stack_name.text or "")
    child_retain_resources = el.find("RetainResources")
    if child_retain_resources is not None:
        import aws_sdk_cloudformation.types.retain_resources

        out["retain_resources"] = (
            aws_sdk_cloudformation.types.retain_resources.deserialize_query(
                child_retain_resources
            )
        )
    child_role_arn = el.find("RoleARN")
    if child_role_arn is not None:
        out["role_arn"] = str(child_role_arn.text or "")
    child_client_request_token = el.find("ClientRequestToken")
    if child_client_request_token is not None:
        out["client_request_token"] = str(child_client_request_token.text or "")
    child_deletion_mode = el.find("DeletionMode")
    if child_deletion_mode is not None:
        import aws_sdk_cloudformation.types.deletion_mode

        out["deletion_mode"] = (
            aws_sdk_cloudformation.types.deletion_mode.deserialize_query(
                child_deletion_mode
            )
        )
    return out
