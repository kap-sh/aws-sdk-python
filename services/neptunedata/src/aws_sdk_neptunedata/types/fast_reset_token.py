"""Generated from Smithy shape ``com.amazonaws.neptunedata#FastResetToken``."""

from typing import TypedDict

from typing_extensions import NotRequired


class FastResetToken(TypedDict):
    token: NotRequired["str"]
    """<p>A UUID generated by the database in the <code>initiateDatabaseReset</code> action, and then consumed by the <code>performDatabaseReset</code> to reset the database.</p>"""


# --- restJson1 ser/de ---
def serialize_json(value: FastResetToken) -> dict:
    out: dict = {}
    if "token" in value:
        out["token"] = value["token"]
    return out


def deserialize_json(data: dict) -> FastResetToken:
    out: FastResetToken = {}  # type: ignore[typeddict-item]
    if "token" in data:
        out["token"] = data["token"]
    return out
