"""Generated from Smithy shape ``com.amazonaws.bedrockagentcorecontrol#MemoryStrategy``."""

from typing import TYPE_CHECKING

from typing_extensions import NotRequired, TypedDict

from capo_bedrock_agentcore_control.errors import DeserializationError

if TYPE_CHECKING:
    import datetime

    import capo_bedrock_agentcore_control.types.description
    import capo_bedrock_agentcore_control.types.memory_record_schema
    import capo_bedrock_agentcore_control.types.memory_strategy_id
    import capo_bedrock_agentcore_control.types.memory_strategy_status
    import capo_bedrock_agentcore_control.types.memory_strategy_type
    import capo_bedrock_agentcore_control.types.name
    import capo_bedrock_agentcore_control.types.namespaces_list
    import capo_bedrock_agentcore_control.types.strategy_configuration


class MemoryStrategy(TypedDict, closed=True):
    strategy_id: (
        "capo_bedrock_agentcore_control.types.memory_strategy_id.MemoryStrategyId"
    )
    """<p>The unique identifier of the memory strategy.</p>"""
    name: "capo_bedrock_agentcore_control.types.name.Name"
    """<p>The name of the memory strategy.</p>"""
    description: NotRequired[
        "capo_bedrock_agentcore_control.types.description.Description"
    ]
    """<p>The description of the memory strategy.</p>"""
    configuration: NotRequired[
        "capo_bedrock_agentcore_control.types.strategy_configuration.StrategyConfiguration"
    ]
    """<p>The configuration of the memory strategy.</p>"""
    type: "capo_bedrock_agentcore_control.types.memory_strategy_type.MemoryStrategyType"
    """<p>The type of the memory strategy.</p>"""
    namespaces: "capo_bedrock_agentcore_control.types.namespaces_list.NamespacesList"
    """<p>This is a legacy parameter. The namespaces associated with the memory strategy.</p>"""
    namespace_templates: (
        "capo_bedrock_agentcore_control.types.namespaces_list.NamespacesList"
    )
    """<p>The namespaceTemplates associated with the memory strategy.</p>"""
    created_at: NotRequired["datetime.datetime"]
    """<p>The timestamp when the memory strategy was created.</p>"""
    updated_at: NotRequired["datetime.datetime"]
    """<p>The timestamp when the memory strategy was last updated.</p>"""
    status: NotRequired[
        "capo_bedrock_agentcore_control.types.memory_strategy_status.MemoryStrategyStatus"
    ]
    """<p>The current status of the memory strategy.</p>"""
    memory_record_schema: NotRequired[
        "capo_bedrock_agentcore_control.types.memory_record_schema.MemoryRecordSchema"
    ]
    """<p>Schema for metadata fields on records generated by this strategy.</p>"""


# --- restJson1 ser/de ---
def serialize_json(value: MemoryStrategy) -> dict:
    out: dict = {}
    out["strategyId"] = value["strategy_id"]
    out["name"] = value["name"]
    if "description" in value:
        out["description"] = value["description"]
    if "configuration" in value:
        import capo_bedrock_agentcore_control.types.strategy_configuration

        out["configuration"] = (
            capo_bedrock_agentcore_control.types.strategy_configuration.serialize_json(
                value["configuration"]
            )
        )
    import capo_bedrock_agentcore_control.types.memory_strategy_type

    out["type"] = (
        capo_bedrock_agentcore_control.types.memory_strategy_type.serialize_json(
            value["type"]
        )
    )
    import capo_bedrock_agentcore_control.types.namespaces_list

    out["namespaces"] = (
        capo_bedrock_agentcore_control.types.namespaces_list.serialize_json(
            value["namespaces"]
        )
    )
    import capo_bedrock_agentcore_control.types.namespaces_list

    out["namespaceTemplates"] = (
        capo_bedrock_agentcore_control.types.namespaces_list.serialize_json(
            value["namespace_templates"]
        )
    )
    if "created_at" in value:
        import capo_bedrock_agentcore_control.types._prelude.timestamp

        out["createdAt"] = (
            capo_bedrock_agentcore_control.types._prelude.timestamp.serialize_json(
                value["created_at"]
            )
        )
    if "updated_at" in value:
        import capo_bedrock_agentcore_control.types._prelude.timestamp

        out["updatedAt"] = (
            capo_bedrock_agentcore_control.types._prelude.timestamp.serialize_json(
                value["updated_at"]
            )
        )
    if "status" in value:
        import capo_bedrock_agentcore_control.types.memory_strategy_status

        out["status"] = (
            capo_bedrock_agentcore_control.types.memory_strategy_status.serialize_json(
                value["status"]
            )
        )
    if "memory_record_schema" in value:
        import capo_bedrock_agentcore_control.types.memory_record_schema

        out["memoryRecordSchema"] = (
            capo_bedrock_agentcore_control.types.memory_record_schema.serialize_json(
                value["memory_record_schema"]
            )
        )
    return out


def deserialize_json(data: dict) -> MemoryStrategy:
    out: MemoryStrategy = {}  # type: ignore[typeddict-item]
    if "strategyId" in data:
        out["strategy_id"] = data["strategyId"]
    else:
        raise DeserializationError("MemoryStrategy.strategy_id required")
    if "name" in data:
        out["name"] = data["name"]
    else:
        raise DeserializationError("MemoryStrategy.name required")
    if "description" in data:
        out["description"] = data["description"]
    if "configuration" in data:
        import capo_bedrock_agentcore_control.types.strategy_configuration

        out["configuration"] = (
            capo_bedrock_agentcore_control.types.strategy_configuration.deserialize_json(
                data["configuration"]
            )
        )
    if "type" in data:
        import capo_bedrock_agentcore_control.types.memory_strategy_type

        out["type"] = (
            capo_bedrock_agentcore_control.types.memory_strategy_type.deserialize_json(
                data["type"]
            )
        )
    else:
        raise DeserializationError("MemoryStrategy.type required")
    if "namespaces" in data:
        import capo_bedrock_agentcore_control.types.namespaces_list

        out["namespaces"] = (
            capo_bedrock_agentcore_control.types.namespaces_list.deserialize_json(
                data["namespaces"]
            )
        )
    else:
        raise DeserializationError("MemoryStrategy.namespaces required")
    if "namespaceTemplates" in data:
        import capo_bedrock_agentcore_control.types.namespaces_list

        out["namespace_templates"] = (
            capo_bedrock_agentcore_control.types.namespaces_list.deserialize_json(
                data["namespaceTemplates"]
            )
        )
    else:
        raise DeserializationError("MemoryStrategy.namespace_templates required")
    if "createdAt" in data:
        import capo_bedrock_agentcore_control.types._prelude.timestamp

        out["created_at"] = (
            capo_bedrock_agentcore_control.types._prelude.timestamp.deserialize_json(
                data["createdAt"]
            )
        )
    if "updatedAt" in data:
        import capo_bedrock_agentcore_control.types._prelude.timestamp

        out["updated_at"] = (
            capo_bedrock_agentcore_control.types._prelude.timestamp.deserialize_json(
                data["updatedAt"]
            )
        )
    if "status" in data:
        import capo_bedrock_agentcore_control.types.memory_strategy_status

        out["status"] = (
            capo_bedrock_agentcore_control.types.memory_strategy_status.deserialize_json(
                data["status"]
            )
        )
    if "memoryRecordSchema" in data:
        import capo_bedrock_agentcore_control.types.memory_record_schema

        out["memory_record_schema"] = (
            capo_bedrock_agentcore_control.types.memory_record_schema.deserialize_json(
                data["memoryRecordSchema"]
            )
        )
    return out
