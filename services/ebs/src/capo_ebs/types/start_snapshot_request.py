"""Generated from Smithy shape ``com.amazonaws.ebs#StartSnapshotRequest``."""

from typing import TYPE_CHECKING

from typing_extensions import NotRequired, TypedDict

from capo_ebs.errors import DeserializationError

if TYPE_CHECKING:
    import capo_ebs.types.boolean
    import capo_ebs.types.description
    import capo_ebs.types.idempotency_token
    import capo_ebs.types.kms_key_arn
    import capo_ebs.types.snapshot_id
    import capo_ebs.types.tags
    import capo_ebs.types.timeout
    import capo_ebs.types.volume_size


class StartSnapshotRequest(TypedDict, closed=True):
    volume_size: "capo_ebs.types.volume_size.VolumeSize"
    """<p>The size of the volume, in GiB. The maximum size is <code>65536</code> GiB (64 TiB).</p>"""
    parent_snapshot_id: NotRequired["capo_ebs.types.snapshot_id.SnapshotId"]
    r"""<p>The ID of the parent snapshot. If there is no parent snapshot, or if you are creating the first snapshot for an on-premises volume, omit this parameter.</p> <p>You can't specify <b>ParentSnapshotId</b> and <b>Encrypted</b> in the same request. If you specify both parameters, the request fails with <code>ValidationException</code>.</p> <p>The encryption status of the snapshot depends on the values that you specify for <b>Encrypted</b>, <b>KmsKeyArn</b>, and <b>ParentSnapshotId</b>, and whether your Amazon Web Services account is enabled for <a href=\"https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/EBSEncryption.html#encryption-by-default\"> encryption by default</a>. For more information, see <a href=\"https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/ebsapis-using-encryption.html\"> Using encryption</a> in the <i>Amazon Elastic Compute Cloud User Guide</i>.</p> <important> <p>If you specify an encrypted parent snapshot, you must have permission to use the KMS key that was used to encrypt the parent snapshot. For more information, see <a href=\"https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/ebsapi-permissions.html#ebsapi-kms-permissions\"> Permissions to use Key Management Service keys</a> in the <i>Amazon Elastic Compute Cloud User Guide</i>.</p> </important>"""
    tags: NotRequired["capo_ebs.types.tags.Tags"]
    """<p>The tags to apply to the snapshot.</p>"""
    description: NotRequired["capo_ebs.types.description.Description"]
    """<p>A description for the snapshot.</p>"""
    client_token: NotRequired["capo_ebs.types.idempotency_token.IdempotencyToken"]
    r"""<p>A unique, case-sensitive identifier that you provide to ensure the idempotency of the request. Idempotency ensures that an API request completes only once. With an idempotent request, if the original request completes successfully. The subsequent retries with the same client token return the result from the original successful request and they have no additional effect.</p> <p>If you do not specify a client token, one is automatically generated by the Amazon Web Services SDK.</p> <p>For more information, see <a href=\"https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/ebs-direct-api-idempotency.html\"> Idempotency for StartSnapshot API</a> in the <i>Amazon Elastic Compute Cloud User Guide</i>.</p>"""
    encrypted: NotRequired["capo_ebs.types.boolean.Boolean"]
    r"""<p>Indicates whether to encrypt the snapshot.</p> <p>You can't specify <b>Encrypted</b> and <b> ParentSnapshotId</b> in the same request. If you specify both parameters, the request fails with <code>ValidationException</code>.</p> <p>The encryption status of the snapshot depends on the values that you specify for <b>Encrypted</b>, <b>KmsKeyArn</b>, and <b>ParentSnapshotId</b>, and whether your Amazon Web Services account is enabled for <a href=\"https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/EBSEncryption.html#encryption-by-default\"> encryption by default</a>. For more information, see <a href=\"https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/ebsapis-using-encryption.html\"> Using encryption</a> in the <i>Amazon Elastic Compute Cloud User Guide</i>.</p> <important> <p>To create an encrypted snapshot, you must have permission to use the KMS key. For more information, see <a href=\"https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/ebsapi-permissions.html#ebsapi-kms-permissions\"> Permissions to use Key Management Service keys</a> in the <i>Amazon Elastic Compute Cloud User Guide</i>.</p> </important>"""
    kms_key_arn: NotRequired["capo_ebs.types.kms_key_arn.KmsKeyArn"]
    r"""<p>The Amazon Resource Name (ARN) of the Key Management Service (KMS) key to be used to encrypt the snapshot.</p> <p>The encryption status of the snapshot depends on the values that you specify for <b>Encrypted</b>, <b>KmsKeyArn</b>, and <b>ParentSnapshotId</b>, and whether your Amazon Web Services account is enabled for <a href=\"https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/EBSEncryption.html#encryption-by-default\"> encryption by default</a>. For more information, see <a href=\"https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/ebsapis-using-encryption.html\"> Using encryption</a> in the <i>Amazon Elastic Compute Cloud User Guide</i>.</p> <important> <p>To create an encrypted snapshot, you must have permission to use the KMS key. For more information, see <a href=\"https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/ebsapi-permissions.html#ebsapi-kms-permissions\"> Permissions to use Key Management Service keys</a> in the <i>Amazon Elastic Compute Cloud User Guide</i>.</p> </important>"""
    timeout: NotRequired["capo_ebs.types.timeout.Timeout"]
    """<p>The amount of time (in minutes) after which the snapshot is automatically cancelled if:</p> <ul> <li> <p>No blocks are written to the snapshot.</p> </li> <li> <p>The snapshot is not completed after writing the last block of data.</p> </li> </ul> <p>If no value is specified, the timeout defaults to <code>60</code> minutes.</p>"""


# --- restJson1 ser/de ---
def serialize_json(value: StartSnapshotRequest) -> dict:
    out: dict = {}
    out["VolumeSize"] = value["volume_size"]
    if "parent_snapshot_id" in value:
        out["ParentSnapshotId"] = value["parent_snapshot_id"]
    if "tags" in value:
        import capo_ebs.types.tags

        out["Tags"] = capo_ebs.types.tags.serialize_json(value["tags"])
    if "description" in value:
        out["Description"] = value["description"]
    if "client_token" in value:
        out["ClientToken"] = value["client_token"]
    if "encrypted" in value:
        out["Encrypted"] = value["encrypted"]
    if "kms_key_arn" in value:
        out["KmsKeyArn"] = value["kms_key_arn"]
    if "timeout" in value:
        out["Timeout"] = value["timeout"]
    return out


def deserialize_json(data: dict) -> StartSnapshotRequest:
    out: StartSnapshotRequest = {}  # type: ignore[typeddict-item]
    if "VolumeSize" in data:
        out["volume_size"] = data["VolumeSize"]
    else:
        raise DeserializationError("StartSnapshotRequest.volume_size required")
    if "ParentSnapshotId" in data:
        out["parent_snapshot_id"] = data["ParentSnapshotId"]
    if "Tags" in data:
        import capo_ebs.types.tags

        out["tags"] = capo_ebs.types.tags.deserialize_json(data["Tags"])
    if "Description" in data:
        out["description"] = data["Description"]
    if "ClientToken" in data:
        out["client_token"] = data["ClientToken"]
    if "Encrypted" in data:
        out["encrypted"] = data["Encrypted"]
    if "KmsKeyArn" in data:
        out["kms_key_arn"] = data["KmsKeyArn"]
    if "Timeout" in data:
        out["timeout"] = data["Timeout"]
    return out
