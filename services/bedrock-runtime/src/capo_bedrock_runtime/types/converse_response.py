"""Generated from Smithy shape ``com.amazonaws.bedrockruntime#ConverseResponse``."""

from typing import TYPE_CHECKING

from typing_extensions import NotRequired, TypedDict

from capo_bedrock_runtime.errors import DeserializationError

if TYPE_CHECKING:
    import capo_bedrock_runtime.types.converse_metrics
    import capo_bedrock_runtime.types.converse_output
    import capo_bedrock_runtime.types.converse_trace
    import capo_bedrock_runtime.types.performance_configuration
    import capo_bedrock_runtime.types.service_tier
    import capo_bedrock_runtime.types.stop_reason
    import capo_bedrock_runtime.types.token_usage


class ConverseResponse(TypedDict, closed=True):
    output: "capo_bedrock_runtime.types.converse_output.ConverseOutput"
    """<p>The result from the call to <code>Converse</code>.</p>"""
    stop_reason: "capo_bedrock_runtime.types.stop_reason.StopReason"
    """<p>The reason why the model stopped generating output.</p>"""
    usage: "capo_bedrock_runtime.types.token_usage.TokenUsage"
    """<p>The total number of tokens used in the call to <code>Converse</code>. The total includes the tokens input to the model and the tokens generated by the model.</p>"""
    metrics: "capo_bedrock_runtime.types.converse_metrics.ConverseMetrics"
    """<p>Metrics for the call to <code>Converse</code>.</p>"""
    additional_model_response_fields: NotRequired["object"]
    """<p>Additional fields in the response that are unique to the model. </p>"""
    trace: NotRequired["capo_bedrock_runtime.types.converse_trace.ConverseTrace"]
    """<p>A trace object that contains information about the Guardrail behavior.</p>"""
    performance_config: NotRequired[
        "capo_bedrock_runtime.types.performance_configuration.PerformanceConfiguration"
    ]
    """<p>Model performance settings for the request.</p>"""
    service_tier: NotRequired["capo_bedrock_runtime.types.service_tier.ServiceTier"]
    """<p>Specifies the processing tier configuration used for serving the request.</p>"""


# --- restJson1 ser/de ---
def serialize_json(value: ConverseResponse) -> dict:
    out: dict = {}
    import capo_bedrock_runtime.types.converse_output

    out["output"] = capo_bedrock_runtime.types.converse_output.serialize_json(
        value["output"]
    )
    import capo_bedrock_runtime.types.stop_reason

    out["stopReason"] = capo_bedrock_runtime.types.stop_reason.serialize_json(
        value["stop_reason"]
    )
    import capo_bedrock_runtime.types.token_usage

    out["usage"] = capo_bedrock_runtime.types.token_usage.serialize_json(value["usage"])
    import capo_bedrock_runtime.types.converse_metrics

    out["metrics"] = capo_bedrock_runtime.types.converse_metrics.serialize_json(
        value["metrics"]
    )
    if "additional_model_response_fields" in value:
        out["additionalModelResponseFields"] = value["additional_model_response_fields"]
    if "trace" in value:
        import capo_bedrock_runtime.types.converse_trace

        out["trace"] = capo_bedrock_runtime.types.converse_trace.serialize_json(
            value["trace"]
        )
    if "performance_config" in value:
        import capo_bedrock_runtime.types.performance_configuration

        out["performanceConfig"] = (
            capo_bedrock_runtime.types.performance_configuration.serialize_json(
                value["performance_config"]
            )
        )
    if "service_tier" in value:
        import capo_bedrock_runtime.types.service_tier

        out["serviceTier"] = capo_bedrock_runtime.types.service_tier.serialize_json(
            value["service_tier"]
        )
    return out


def deserialize_json(data: dict) -> ConverseResponse:
    out: ConverseResponse = {}  # type: ignore[typeddict-item]
    if "output" in data:
        import capo_bedrock_runtime.types.converse_output

        out["output"] = capo_bedrock_runtime.types.converse_output.deserialize_json(
            data["output"]
        )
    else:
        raise DeserializationError("ConverseResponse.output required")
    if "stopReason" in data:
        import capo_bedrock_runtime.types.stop_reason

        out["stop_reason"] = capo_bedrock_runtime.types.stop_reason.deserialize_json(
            data["stopReason"]
        )
    else:
        raise DeserializationError("ConverseResponse.stop_reason required")
    if "usage" in data:
        import capo_bedrock_runtime.types.token_usage

        out["usage"] = capo_bedrock_runtime.types.token_usage.deserialize_json(
            data["usage"]
        )
    else:
        raise DeserializationError("ConverseResponse.usage required")
    if "metrics" in data:
        import capo_bedrock_runtime.types.converse_metrics

        out["metrics"] = capo_bedrock_runtime.types.converse_metrics.deserialize_json(
            data["metrics"]
        )
    else:
        raise DeserializationError("ConverseResponse.metrics required")
    if "additionalModelResponseFields" in data:
        out["additional_model_response_fields"] = data["additionalModelResponseFields"]
    if "trace" in data:
        import capo_bedrock_runtime.types.converse_trace

        out["trace"] = capo_bedrock_runtime.types.converse_trace.deserialize_json(
            data["trace"]
        )
    if "performanceConfig" in data:
        import capo_bedrock_runtime.types.performance_configuration

        out["performance_config"] = (
            capo_bedrock_runtime.types.performance_configuration.deserialize_json(
                data["performanceConfig"]
            )
        )
    if "serviceTier" in data:
        import capo_bedrock_runtime.types.service_tier

        out["service_tier"] = capo_bedrock_runtime.types.service_tier.deserialize_json(
            data["serviceTier"]
        )
    return out
