{
  "name": "aws-sdk-python",
  "private": true,
  "workspaces": [
    "services/*"
  ],
  "scripts": {
    "version:packages": "changeset version && python scripts/sync_pyproject.py && python scripts/aggregate_changelog.py"
  },
  "devDependencies": {
    "@changesets/changelog-github": "^0.7.0",
    "@changesets/cli": "^2.31.0",
    "@commitlint/cli": "^18.4.3",
    "@commitlint/config-conventional": "^18.4.3"
  }
}
